"""Pluggable reasoning modules for the planner harness (ADR-102).

The contract lives in ``base.py``; implementations register in
``registry.py``; per-kind pinned/auto resolution lives in ``selector.py``.
"""
